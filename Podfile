# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'whats in the picture' do
  use_frameworks!
  pod 'AWSCore', '~> 2.12.2'
  pod 'AWSAppSync', '~> 3.0.0'

end
